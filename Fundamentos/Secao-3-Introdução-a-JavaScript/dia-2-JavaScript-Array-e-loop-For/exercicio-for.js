let groceryList = ['Arroz', 'Feijão', 'Alface', 'Melancia'];

for (let indice = 0; indice < groceryList.length; indice += 1) {
  let ingredientes = groceryList[indice];
  console.log(ingredientes);
}